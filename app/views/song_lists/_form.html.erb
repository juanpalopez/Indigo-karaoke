<%= form_for(@song_list) do |f| %>
  <% if @song_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song_list.errors.count, "error") %> prohibited this song_list from being saved:</h2>

      <ul>
      <% @song_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :hour %><br>
    <%= f.time_select :hour %>
  </div>
  <div class="field">
    <%= f.label :client_id %><br>
    <%= f.text_field :client_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
